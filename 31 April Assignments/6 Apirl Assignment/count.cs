//Write a method that calculates the sum of digits in a number.
using System; 

class countwords{ 
      static void Main(string[] args){ 
            
                string str = "Learning Cl in fun"; 
                int count = 0; 
                for (int i = 0; i < str.Length; i++) { 
                    if (str[i] == ' ') { 
                        count++; 
                    } 
                } 
                Cons 
                ole.WriteLine("Number of words: " + (count + 1));
      } 
      
}   

